# Local dev — leave VITE_API_URL unset.
# Vite proxies /api/* to the backend automatically (no CORS, no port config).
VITE_PORT=5273

# Production only — point to the deployed backend:
# VITE_API_URL=https://your-backend.onrender.com
