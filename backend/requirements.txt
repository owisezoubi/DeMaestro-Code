# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Schema validation & settings ---
pydantic[email]==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Firebase ---
firebase-admin==6.5.0

# --- AI SDKs (used in later weeks) ---
google-generativeai==0.8.3
claude-agent-sdk==0.1.0
anthropic>=0.102.0

# --- File / PDF handling ---
PyMuPDF==1.25.1

# --- Sandbox (later weeks) ---
docker==7.1.0

# --- Database (direct psycopg2 for per-project DB creation) ---
psycopg2-binary==2.9.9

# --- Logging & observability ---
structlog==24.4.0
sentry-sdk[fastapi]==2.14.0

# --- Dev / test ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
ruff==0.6.8
black==24.10.0
flake8==7.1.1
playwright>=1.44.0
