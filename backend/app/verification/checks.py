"""Static verification checks. Implemented in Week 5–6.

Planned checks:
  - File completeness (entry points, package manifests).
  - Frontend API calls match backend routes (NFR3).
  - DB migrations / schema present.
  - Generated docker-compose.yml present and parses.
"""


def run_all(project_dir) -> dict:
    """Run every static check and return a structured report."""
    raise NotImplementedError("Implemented in Week 5")
